name: First worlflow
on: workflow_dispatch
jobs: 
    first-job:
      runs-on: ubuntu-latest
      steps:
        - name: print greeting 
          run: echo "Hello World"
        - name: print Goodbye
          run: echo "Done, bye"
